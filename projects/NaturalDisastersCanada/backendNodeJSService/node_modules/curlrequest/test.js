var curl = require('./');

var options = {
    url: 'http://node.io'
};

(function next() {
    curl.request(options, function (err, data, meta) {
        console.log(meta.args);
        next();
    });
})();
